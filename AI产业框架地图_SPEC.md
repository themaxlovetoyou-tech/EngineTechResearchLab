# AI 产业框架地图 · 工程规格 (SPEC)

> 这份文档是为长上下文对话中的 AI 协作者准备的"项目说明书"。每次接手前先读它。
> 文档与代码同步：改了代码必须同步修文档；改了文档但没动代码，要标记 `[PENDING]`。
>
> **当前版本：v1.6 · 2026-06-20**
> **历史版本：见 `backups/` 与 `CHANGELOG.md`**
>
> 最高准则见 §0「顶层设计原则（宪法）」；动手扩展前先过 §0.5 那道闸。
> v1.6：L10–L12 按"科斯脊柱"重构——L10 AI 原生执行单元（存量重构）/ L11 AI 原生公司（重画边界）/ L12 AI 原生经济（边界溶解），由 L9 Agent 可靠性驱动；并修复"近期动态"锚点跳进折叠批次的交互 bug。

---

## 0. 顶层设计原则（宪法）

> 本节是整份 SPEC 的最高约束，优先级高于其后所有章节。任何新板块、新维度、新切片要进地图，先过 §0.5 的闸。
> 来源：一轮关于"地图后续迭代方向"的顶层讨论（2026-06）。这些是已沉淀、不再反复讨论的方向性结论。

### 0.0 一句话定位

一张可持续维护的 AI 产业「活地图」：把产业拆成 12 层 × 4 大族群，每周由自动任务采集动态、刷新数据，让读者从"骨架 + 经络"两个角度同时理解产业当前的形状与正在变化的张力。

### 0.1 服务对象（恒定）

唯一服务对象：**未来二十年在 AI 生态下不断涌现的、有思维框架的 AI 创业者**。它是支撑投资与创业决策的**决策工具**，不是大众认知读物，也不是私人思考笔记。

服务对象最终细分为三类——**进场者**（找入口/窗口）、**在位者**（看上下游谁会吃掉我）、**平台者**（看协议/标准之争）——但通过"**同一张地形、不同读法**"实现，**不为三类各做一套**。

### 0.2 双重功能

1. **定位器**：让创业者随时知道"我现在做的事在产业链的哪里"。这是基础作用，再优秀的创业者也需要常看地图。
2. **带立场的观点集**：价值必然带来立场，关键是"对谁有价值"。

策略上**主动慢下来**：快流（新闻/信息流）创业者自己就有，不构成来这儿的理由；地图的稀缺性在于**把快流蒸馏成结构性结论**——沉淀比刷新更重要。

### 0.3 核心边界（本宪法最重要的一条）

> **地图只把"公共的山川"画到极致（事实 + 公共判断），坚决不替读者做需要"我是谁"才能算的私有去留决定。**

这条边界同时是三条线：

- **内容线**：本体只装对所有创业者都成立的信息。"模型在向上吞并中间层"对谁都成立 → 公共判断，进；"所以你别在 L5 创业" → 私有结论，不进。
- **价值线**：公共判断是地图护城河（免费/付费皆可，是本体）；私有判断（结合"你是谁"算出的去留与押注）才是真正稀缺、可售卖的。
- **信任线**：服务对象最反感被替决定，"不替你做去留决定"正是赢得这类人信任的前提。

**背书关系（关键）**：公共与私有不是切割，是背书——公共山川画得越深，私有判断越值钱，因为它是判断力的公开证据。所以短期最该投入的，恰恰是把地图本体（公共判断）做到无人能及。

### 0.4 不变量清单

1. **主轴恒为 12 层**技术栈分层；结构（原 §01「是什么」）与价值流向判断（原 §02「怎么流、什么张力」）在宏观层是**一体两面**，应整合呈现，§02 不是配角。
2. **判断是护城河，骨架是大宗商品**。判断分两向：**层间关系**（横向）+ **层内垂直拆分**（纵向）；但只收录**公共判断**。
3. **核心边界**（见 §0.3）：只画公共山川，不替读者做私有去留决定。
4. 公共判断越深，私有判断越值钱——**背书关系，非切割关系**。
5. **服务对象恒定**为未来二十年的 AI 创业者，细分进场/在位/平台三类，通过"同一张地形、不同读法"服务。
6. **重心向 L10–L12（AI 重构经济）倾斜**——那是多数未来创业者真正的土壤。
7. **"角色判断"（旁观/参战/拓荒）必须内化，不进结构**。它不是层的固有属性，而是"层 × 观察者"的交叉产物，属私有计算，地图只给原料。

### 0.5 进图判定闸（每次扩展前必过）

任何要进地图的内容，先回答一个问题：

> **这条信息，是对所有创业者都一样（→ 进地图本体），还是需要"我是谁"才能算（→ 留给读者，或进未来的私有判断层）？**

动词是试金石：**陈述地形**的动词（谁在守、窗口开/关、护城河多深、什么变化冲击结构）是公共的，进；**祈使/预测/派活**的动词（你该做、值得做、机会在、将出现、你去 L11）是私有的，不进。

---

## 1. 文件结构

| 文件 | 角色 | 是否同步 |
|---|---|---|
| `AI产业框架地图.html` | 主页面，单文件交付 | 主产物 |
| `AI产业框架地图_SPEC.md` | 本文件，工程规格 | 跟主页面同步 |
| (调度) `mnt/uploads/SKILL.md` 中的 `ai-stack-weekly-pulse` | 每周自动维护任务定义 | 由调度系统读取 |

主页面是**单文件 HTML**——所有 CSS 与 JS 内嵌，便于拷贝/分享/离线打开。
不要拆分成多文件，除非项目从"可分享的研究读物"演化为"可部署的 Web App"。

---

## 2. 顶层信息架构

页面分 6 段，按出现顺序：

| 段 | 锚点 | 角色 | 是否动态 |
|---|---|---|---|
| 顶部 Header | `#top` | 标题 + 元信息（编制 / 最近更新 / 层数 / 玩家数） | 元信息按周更新 |
| §00 产业动态 Pulse | `#pulse` | 动态流，每周追加一批，旧批次默认折叠 | **每周自动** |
| §01 全栈一览 | `#stack` | 12 层骨架 + 缩略 SVG | 静态 |
| §02 层级关系 | `#relations` | 价值流 / 动态 / 张力 / 压缩链 | 静态 |
| §详情 12 层 | `#details` `#l1..#l12` | 12 个长卡片（含 3 层玩家结构 / 规模 / 6 维评分 / 机会窗） | 数字与玩家季更 |
| §03 缺失维度 | `#dimensions` `#dim-a #dim-s #dim-b #dim-c` | 治理(3) + 结构性力(3) + 切片(行业/地理) + 资本 | 半年级 |
| Footer | — | 使用说明、阅读路径、数据来源 | 静态 |

固定顶部导航 `.topnav` + 右侧抽屉式目录 `.toc-overlay` 横跨所有段。

---

## 3. 数据约定（最重要的一节）

### 3.1 族群与层级

四大族群，颜色与 CSS 类名固定：

| 族群 | CSS 类 | 颜色变量 | 包含层级 | 中文名 |
|---|---|---|---|---|
| g1 物理基础 | `g1` | `--g1` 赤陶 | L1–L3 | 能源 / 芯片 / 基础设施 |
| g2 模型与能力 | `g2` | `--g2` 深海 | L4–L7 | 大型语言模型 / 上下文 / 长期存储 / 工具集成 |
| g3 智能体 | `g3` | `--g3` 森林 | L8–L9 | 智能体框架 / 智能体 |
| g4 经济组织 | `g4` | `--g4` 陈金 | L10–L12 | AI 原生执行单元 / 公司 / 经济（v1.6 重命名）|

**约定**：任何引用层级的地方，标号必须是 `L<数字>`（大写 L），可通过正则 `/L\s*(\d+)/g` 解析。
JS 中已有的层级映射函数 `groupOfNum(n)` 是单一事实来源。

### 3.2 动态流 (Pulse) 条目结构

```html
<article class="pulse-entry g<N>">             <!-- N = 1..4 族群编号 -->
  <div class="pulse-entry-layer">L?<br>层名</div>  <!-- 可多层："L8 · L9" -->
  <div>
    <h3 class="pulse-headline">标题</h3>
    <p class="pulse-text">说明（1–2 句）</p>
    <a class="pulse-src" href="..." target="_blank" rel="noopener noreferrer">来源名 ↗</a>
  </div>
</article>
```

批次结构：

```html
<div class="pulse-batch">
  <div class="pulse-batch-head">
    <span class="pulse-batch-date">YYYY.MM.DD</span>
    <span class="pulse-batch-label">第 N 批</span>
    <span class="pulse-batch-count">X 条</span>
    <!-- pulse-batch-summary 由 JS 注入，不要手写 -->
  </div>
  <article class="pulse-entry ...">...</article>
  ...
</div>
```

**标记锚点**（自动任务依赖）：

- `<!-- PULSE-FEED:START -->` / `<!-- PULSE-FEED:END -->`
- `id="headerLastUpdate"` / `id="pulseLastUpdate"` / `id="pulseChangeCount"` / `id="pulseBatchCount"`

绝对不要删除这 4 个 id 或两个 HTML 注释标记，否则每周任务会断。

### 3.3 12 层卡片

每个层卡片是 `<article class="layer g<N>" id="l<num>">`，结构：

```
.layer
  .layer-marker (大数字 + 竖线)
  .layer-content
    .layer-head (标题 + 标签)
    [由 JS 注入] .layer-recent  ← 自动收集相关 pulse
    .layer-desc (一句话)
    .layer-body
      .field (描述)
      .field 玩家 → .players-tiered (头部 / 挑战者 / 悬疑) ← v1.2 新结构
      .field 规模 (.scale-grid)
    [由 JS 注入] .layer-scorecard ← 6 维评分
    .opportunity (窗口/地形块) ← v1.4 改造：CSS 类名仍为 .opportunity，可见标签改为「窗口 · Window」
    [由 JS 注入] .layer-mininav  ← 上一层 / 顶部 / 下一层
```

> **§3.3.0 `.opportunity` 内容约定（v1.4 · 过 §0.5 闸后）**：这一块是**客观地形**，不是创业建议。格式恒为「`<strong>` 窗口状态（开 / 收窄 / 关）`</strong>` + 一句结构性原因 + 客观地带枚举」。**禁止**祈使 / 预测 / 派活动词（你该做、值得做、机会在、将出现、下一个 X 会是谁）——这些是私有判断，留给读者。moat / 资本 / 谁在守已分别在速测和 `players-tiered` 里，本块只补"窗口"这一维。

#### 3.3.1 玩家三层结构（v1.2）

每层玩家区使用 `.players-tiered` 三栏：

| 栏 | CSS 类 | 内容 | 字数 |
|---|---|---|---|
| 头部 | `.ptier-top` | 3–7 家定义这一层的力量；保留 `.player.lead` 主导者 | 1–3 字玩家名 + 可选 `<span class="tag">` |
| 挑战者 | `.ptier-mid` | 2–3 家正在改写假设；每家配 `.ptier-row` + `.ptier-note` 一句话注释（含 `<em>` 关键词） | 注释 < 30 字 |
| 悬疑 | `.ptier-question` | 1 段「未来 12–18 个月最可能颠覆现状的问题」，含 `<strong>` 关键判断与 `<em>` 推论 | 60–120 字 |

### 3.4 6 维层级评分

在 JS 中 `scores` 表是单一事实来源，键名固定：

| 维度 | 范围 | 含义 |
|---|---|---|
| moat | 0–5 | 护城河深度 |
| commod | 0–5 | 商品化压力（越高越被压扁） |
| risk | 0–5 | 5 年风险 |
| cycle | 0–5 | 客户决策周期（越长越高） |
| capital | 0–5 | 资本密度 |
| growth | 0–5 | 当前增速 |

更新规则：当 pulse 流连续 3-4 期影响同一维度（例如开源模型连续突破压低 L4 的 moat），就调整 1 档；不要频繁微调。

---

## 4. CSS 架构与已知技术债

### 4.1 颜色 / 字体 / 尺寸 token

`:root` 已定义：

| 类别 | Token |
|---|---|
| 颜色 | `--bg --bg-deep --ink --ink-soft --ink-mute --line --line-soft --accent --accent-deep --paper` |
| 组件色（v1.4.1 新增） | `--nav-bg`（顶栏底色）`--invert-bg --invert-ink`（dim-closing 反色面板）；三者在亮/暗 `:root` 各自定义，确保暗色下 nav 与反色面板不再"浮白" |
| 族群 | `--g1..--g4` + `--g1-bg..--g4-bg` |
| 字体 | `--display --body --mono --cn-display` |
| 尺寸（v1.2 新增） | `--nav-h --pad-section --pad-section-sm --pad-container --pad-card --gap-grid --gap-grid-sm --gap-chip --touch-target` |
| 字号（v1.2 新增） | `--fs-h1 --fs-h2 --fs-h3 --fs-h4 --fs-body --fs-small --fs-mono` |

断点里**只覆盖 token 值**，不再覆盖具体规则（理想态）。当前已在 1100 / 700 / 420 / 380 四个断点声明 token 覆盖；高层规则（`.container`, `h1.title`, `.nav-container`, 等）已切换到 `var(--token)`。

`@media (prefers-color-scheme: dark)` 内已重新映射颜色 token，**保留族群语义**。
新增组件应只用变量，不要硬编码颜色或尺寸。

### 4.2 已知债务（按优先级）

| # | 项 | 状态 | 描述 |
|---|---|---|---|
| 1 | 尺寸 token 化 | **PARTIAL** | v1.2 已引入完整 token 集 + 4 个断点的 token 覆盖；但 99 个旧 `!important` 仍存在（主要在 700px 媒体查询中覆盖局部规则）。下一步：逐节把 `font-size: Xpx !important` 替换为 `font-size: var(--fs-N)` |
| 2 | 时间维度 | PENDING | 缺少 2017→2026 时间带，无法可视化"每层重心如何移动" |
| 3 | §04 失败档案 | PENDING | 全篇是赢家叙事，缺少"被压扁的层 / 被并购消失的玩家" |
| 4 | 3 个新横向维度 | **DONE (v1.2)** | 人才流 / 开源份额 / 能耗，已陈列在 §03 dim-s 块 |
| 5 | 资料源「层 × 来源」矩阵 | PENDING | 每个 `scale-num` 应有 footnote 编号 |
| 6 | 玩家三层结构化 | **DONE (v1.2)** | 12 层全部完成"头部 / 挑战者 / 悬疑"三栏 |
| 7 | 暗色模式手动切换 | PENDING | 目前只跟随 OS，没有按钮 |
| 8 | OG 卡片图 | PENDING | meta 已加，但没真的图片资源 |
| 9 | 层评分动态化 | PENDING | 6 维评分目前是硬编码的静态判断，未来应跟随 pulse 流自动微调 |

---

## 5. JS 行为

主脚本是页面尾部单一 IIFE。注入顺序（重要）：

1. `buildObserver()` — 滚动监听 + 响应式 rootMargin
2. 全局 click 拦截器（冒泡阶段、不 stopPropagation）
3. TOC 抽屉控件
4. `onScroll` 与滚动节流
5. `setupPulseCollapse()` — **新加**: 折叠旧批次、注入工具条、持久化 localStorage 偏好
6. `injectScorecards()` — 6 维评分卡片
7. `wirePulseToLayers()` — 给 pulse 打 `data-layers`，给层卡注入「近期动态」
8. `injectLayerMiniNav()` — 层卡间迷你导航

注入函数都是幂等假定：**只调一次**，不要在用户交互后重复调用。

### 5.1 localStorage 键名

| 键 | 值 | 含义 |
|---|---|---|
| `pulseFeedMode` | `'compact'` \| `'expanded'` | 动态流默认显示模式 |

---

## 6. 自动维护任务约定

`ai-stack-weekly-pulse` 任务每周运行，逻辑：

1. 用 Glob 找到 `AI产业框架地图.html`
2. 用 WebSearch 搜过去 7 天 AI 产业变化（6–10 次搜索）
3. 在 `PULSE-FEED:START` 后插入新批次（5–9 条），每条带源 URL
4. 更新 4 个状态 ID：`headerLastUpdate / pulseLastUpdate / pulseChangeCount / pulseBatchCount`
5. 谨慎校准 12 层正文里过时的具体数字（宁可不动）
6. 控制长度：超过 10 个批次时删除最旧的
7. **（发布后）同步发布副本并重新部署**：`cp AI产业框架地图.html index.html` → `git add/commit/push` → GitHub Pages 自动重建。详见 `DEPLOY.md`。
8. **（发布后）部署前过 §9 验证**：HTMLParser、PULSE 标记、4 状态 ID、12 层 ID、死链体检；不过则不推送。

**采集与筛选铁律（v1.5 起，最高优先）**：

- **先过 §0.5 闸**：只收对所有创业者成立、且足以冲击某层结构/张力的**公共**事件；噪音（常规版本号、旧闻、无日期预测、榜单数字）一律不收。**宁缺勿滥**。
- **只收过去 7 天的真事件**：每条必须能定位到本周日期；旧闻（哪怕本周被人转述）不算。逐条核对来源 URL 可达、内容对得上。

**任务行为不变量**：

- 不动 CSS、JS、页面结构
- 不动 `<!-- PULSE-FEED:START/END -->` 标记
- 每条动态必有真实来源 URL，数字模糊时如实表述
- 全程无需用户介入（autonomous）

---

## 7. 当前内容快照

- 编制日期: 2026.05
- 当前版本: **v1.6 · 2026-06-20**（L10–L12 科斯脊柱重构 + 命名 + 锚点交互修复；含 v1.5 发布准备、v1.4.1 暗色、v1.4 边界落地）
- L10–L12 框架: 执行单元(存量重构) → 公司(重画边界) → 经济(边界溶解)，由 L9 Agent 可靠性驱动
- 动态流: 4 批 · 共 28 条（第 4 批 2026.06.16 · 7 条 由每周自动任务追加，**尚未人工核对**；第 3 批 06.08 · 5 条；第 2 批 06.01 · 7 条；首期 05.26 · 9 条）
- 主页面最近更新: 2026.06.16 (第 4 批)
- 12 层卡片: 全部到位 + 全部三层化玩家结构
- 12 层「窗口 · Window」块: 全部由"创业切入口"地形化（仅公共地形，无私有建议）
- L10–L12「需求侧摩擦」field: 已补（采用摩擦 / 价值重新结算 / 冲击谁）
- §03 维度卡: 6 张 hdim-card（治理 3 + 结构性力 3）
- 已实施速测: 12 层全部
- 已实施层间小导航: 12 层全部
- 已实施 pulse↔layer 联动: 12 层全部
- 设计 token: 48 条 `:root` 变量；565 处 `var()` 引用

---

## 8. 设计语言（不要破坏）

- 视觉风格："杂志式产业研究"，不是仪表盘。版面密度高、衬线斜体强调词、纸面颗粒底纹。
- 字体: 中文 Noto Serif SC + 英文 Fraunces (display) / Inter (body) / JetBrains Mono (mono)。
- 留白宁宽不窄。所有新增组件应保留同样的边距与字号节奏。
- 不要加 emoji。所有"强调"应通过 `.accent` 类或 `em` 斜体，不通过表情或颜色噪点。
- 任何用户可见文字，中文为主、英文小副标在斜体。

---

## 9. 验证清单（每次大改后必跑）

- [ ] Python `HTMLParser` 跑 0 错误
- [ ] `PULSE-FEED:START` / `PULSE-FEED:END` 标记仍在
- [ ] 4 个状态 ID 仍在且值正确
- [ ] `id="l1"..id="l12"` 12 个层 ID 都在
- [ ] `</body></html>` 结束、IIFE 闭合
- [ ] 浏览器无 console error
- [ ] 700px 与桌面端断点视觉无明显回退
- [ ] 暗色模式（OS 切换）下所有族群仍可读

---

## 10. 已决问题（不要再讨论）

- 不引入构建工具（Webpack/Vite），保持单文件可分享
- 不引入框架（React/Vue），原生 HTML+CSS+JS
- 不加 cookie / 追踪脚本
- 不引入外部图片资源（OG 卡片例外，但尚未做）
- 字体只用 Google Fonts CDN
- localStorage 仅用于偏好持久化，不保存任何用户数据

---

## 11. 下一步候选（按建议优先级）

✅ 已完成：玩家三层化、3 个结构性力维度、token 系统首版

> **所有待办先过 §0.5 的闸。** 已过闸的方向性结论：
> - **D（L10–L12 补深）→ DONE（v1.4，首期）**：三层各加「需求侧摩擦」field，补"采用摩擦 / 价值重新结算 / 冲击谁"，全是公共山川。重心倾斜的依据见 §0.4-6。后续仍可继续加深。
> - **C（创业切入口 → 客观地形）→ DONE（v1.4）**：12 层 `.opportunity` 已重构为「窗口 · Window」地形块（窗口状态 + 结构性原因 + 客观地带），剥除全部祈使/预测/派活动词。进场/在位/平台三类从同一张地形各读各的含义。三类的**纵向再拆分**仍是后续迭代。
> - **层内"事实/判断"区分 → 呈现层问题**：事实与公共判断都进本体，私有结论一律不进。

待办：

1. **CSS token 完全化**（PARTIAL → DONE）：剩余 99 个 `!important` 替换为 token + 移除媒体查询里硬编码的 `font-size/padding`
2. **2017→2026 时间带**：12 层每层标 3–5 个关键拐点
3. **§04 失败档案**：被压扁的层、被并购消失的玩家、估值修正
4. **资料源「层 × 来源」矩阵**：给每个 scale-num 加 footnote 编号
5. **暗色模式手动切换按钮** + OG 卡片图
6. **层评分动态化**：把 6 维评分从硬编码改为"pulse 流自动微调"机制
7. **密度切换**（dataset.density='compact'）：token 系统已就位，可低成本实现

每完成一项，请同步更新本文档 §4.2、§7、§11 与 `CHANGELOG.md`。
