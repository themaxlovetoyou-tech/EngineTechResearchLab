# AI 产业框架地图 · 版本历程

> 备份在 `backups/`，文件命名 `<名>_v<版本>_<日期>.<后缀>`。
> 每次发布一个新版本时，旧版本必须先备份到 `backups/`，再开始修改主文件。

---

## v1.6 · 2026-06-20（已发布）

**主题**：L10–L12 按"科斯脊柱"重构 + 重命名 + 一个交互 bug 修复。

- **L10–L12 重命名 + 重定义**（全站统一：TOC / §01 全栈 / §02 关系 / 卡片标题 / pulse 标签 / 交叉引用）：
  - L10 「AI native 部门 / AI-Native Departments」→ **「AI 原生执行单元 / AI-Native Execution Units」**。本质：AI 首先重构的不是"部门"，而是"完成一件事的最小执行单元"；存量组织内部被重写（边界内部）。tag → 存量重构。
  - L11 「AI native 公司 / AI-Native Companies」→ **「AI 原生公司 / AI-Native Firms」**。本质：执行单元廉价化后公司边界被重画，组织收缩到不可被 Agent 替代的内核（科斯边界坍缩）。tag → 重画边界。
  - L12 「AI native 经济生态 / Agentic Economy」→ **「AI 原生经济 / Agentic Economy」**。本质：交易成本趋零，公司与市场界限溶解，经济原语从"企业"退回"交易"。tag → 边界溶解。
  - 三层 layer-desc 全部改写为新本质；脊柱：单元→边界→市场（科斯被 AI 逐级击穿），由 L9 Agent 可靠性这一阀门驱动。
- **交互 bug 修复**：layer 卡片"近期动态 · 查看 ↗"锚点指向 `#pulse-N`，但旧批次默认折叠（`.pulse-batch.collapsed > .pulse-entry { display:none }`），点击会跳到隐藏元素、看不到内容。修复：点击锚点前，若目标在折叠批次内则先展开该批次再滚动。
- **性能/清理**：移除 `setupPulseCollapse` 里一段对全页 `querySelectorAll('*')` 扫描却从未使用的死代码（`startMarker`）。
- 旧版备份 `backups/AI产业框架地图_v1.5_2026-06-08.html`；SPEC 头部、§3.1、§7 同步；版本三处 bump v1.5→v1.6。
- 验证：HTMLParser 0 错误、div 收支平衡、JS 两处改动经 Read 核对括号闭合、新名 0 残留旧名。

## v1.5 · 2026-06-08（已发布 · 发布准备）

**主题**：上线前硬化 —— 社交分享、合规、部署回路。

- **OG 分享图** — 生成 1200×630 品牌图 `og.png`（栈 Logo + 12层×4族群 motif），接入 `og:image`/`twitter:image`/`og:image:alt`；补 `og:url` + `canonical`（含 `REPLACE-WITH-YOUR-DOMAIN` 占位，发布时回填绝对域名）。
- **favicon** — 内联"A"换成与 nav 一致的栈形 SVG。
- **免责声明** — footer 加"本图提供事实与公共判断，不构成投资或法律建议"，呼应 §0.3。
- **第 3 批事实核对** — 逐条验证来源 URL：删 Apple×Gemini 条目（实为 2026.01 旧闻、非本周事件，违 §0.5"只收 7 天真事件"）；修正 OpenAI IPO 措辞（据 TechCrunch 为"预计递交"）与 DNS-AID 日期（Linux 基金会 5/27 接纳、Infoblox 6/3 生产）。第 3 批 6 → **5 条**，状态 ID 同步。
- **部署方案** — 新增 `DEPLOY.md`（GitHub Pages 发布 + 自动重新部署回路）；SPEC §6 增第 7–8 步与"采集筛选铁律"（先过 §0.5 闸、只收 7 天真事件）。
- **对比度验证** — 改动的 nav/dim-closing/logo 在明暗两模式全部 ≥ WCAG AA（暗色 nav 文字 15.2:1、dim-closing 14.8:1）。
- 版本三处 bump v1.4.1→v1.5；旧版已备份。
- **遗留（需真机/用户）**：真机多浏览器+移动端渲染测试、回填正式域名、配置 git push 自动部署凭据。

## v1.4.1 · 2026-06-08（已发布 · 补丁）

**主题**：暗色模式一致性修复 + 左上角 Logo。另含本周第 3 批动态。

- **暗色"浮白"修复** — 两处组件在暗色下仍是浅色块、与深色主体割裂：
  - `.topnav`：底色原为硬编码浅米色 `rgba(241,235,221,.92)`，暗色覆盖（早期声明）被后置基础规则按源序覆盖而失效。改为新 token `--nav-bg`（亮/暗 `:root` 各定义），删除失效的直写覆盖。
  - `.dim-closing`（反色收束面板）：原用 `--ink`/`--paper`，二者在暗色全翻转 → 变成"浅底深字"的浮白块。新增 `--invert-bg`/`--invert-ink`，暗色下面板保持深色（`#2a2017`）+ 浅字，与主体一致。
- **Logo** — nav 左上「A」占位替换为内联 SVG：四条自下而上收窄的横条（4 族群/分层栈意象），`fill: currentColor` 跟随 `--ink` 明暗翻转，无外部图片资源（符合 §10）。
- **本周动态** — 第 3 批（2026.06.08）按 §0.5 闸严筛后并入；4 状态 ID 更新。（注：v1.5 事实核对后由 6 条删为 5 条）
- SPEC §4.1 token 表新增"组件色"行；§7 快照更新。版本三处 bump v1.4→v1.4.1。
- 旧版已备份 `backups/AI产业框架地图_v1.4_2026-06-08.html`。

## v1.4 · 2026-06-07（已发布）

**主题**：边界落地 —— 第一次把 §0 宪法应用到 HTML 本体。两件事：地形化 + 经济层补深。

- **A. `.opportunity` 地形化（过 §0.5 闸，对应 §11-C，DONE）** — 12 层「创业切入口 · Opportunity」全部改为「**窗口 · Window**」客观地形块：格式统一为「`<strong>`窗口状态（开/收窄/关）+ 结构性原因 + 客观地带枚举」。剥除全部祈使/预测/派活动词（值得做、机会在、将出现等）。CSS 类名 `.opportunity` 不动。
  - 三条边缘案例裁决落地：L12 删「下一个 Stripe/PayPal 将出现在这一层」；L10「boutique 将大量涌现」软化为趋势陈述；L11「distribution 成护城河」保留（公共判断）。
- **B. L10–L12 需求侧补深（过闸，对应 §11-D，DONE 首期）** — 三层右栏各加一个「**需求侧摩擦 · Adoption Friction**」field，补此前缺失的需求侧视角：采用为何卡住（责任归属/集成成本/内部抵触/信任门槛/监管真空）、价值如何重新结算、谁被冲击。全部公共山川。
- **C. SPEC 同步** — §3.3 加 `.opportunity` 内容约定（§3.3.0）；§11 的 C/D 两项标 DONE；§7 快照更新；三处可见版本号 v1.2→v1.4。
- **验证** — HTMLParser 0 错误、div 收支平衡、PULSE 标记与 4 状态 ID 完好、l1–l12 齐、IIFE 闭合、无残留旧标签/踩线动词。
- 旧版 HTML 已备份至 `backups/AI产业框架地图_v1.2_2026-06-07.html`。

## v1.3 · 2026-06-07（已发布 · 仅 SPEC）

**主题**：确立顶层设计原则（宪法）。**HTML 本体未改动**——本版只动 SPEC，用一轮顶层方向讨论的结论约束后续每次迭代。

- **新增 SPEC §0「顶层设计原则（宪法）」** —— 由原「§0 一句话定位」扩写而来，置于全文最前、优先级最高，含：
  - §0.0 一句话定位（原文保留）
  - §0.1 服务对象：唯一锁定"有思维框架的 AI 创业者"，决策工具而非读物；细分进场/在位/平台三类，但"同一张地形、不同读法"，不为三类各做一套。
  - §0.2 双重功能：定位器 + 带立场的观点集；策略上主动"慢下来"，把快流蒸馏成结构性结论。
  - §0.3 **核心边界**：只画"公共的山川"（事实 + 公共判断），不替读者做需要"我是谁"才能算的私有去留决定。三条线（内容/价值/信任）+ 背书关系（公共越深、私有越值钱）。
  - §0.4 七条不变量清单。
  - §0.5 **进图判定闸**：每次扩展前必过——对所有创业者一样 → 进本体；需"我是谁"才能算 → 留给读者。动词试金石（陈述地形=公共，祈使/预测/派活=私有）。
- **§11 待办** 全部挂到 §0.5 闸下，标注 D（进）/ C（改造为地形块）/ 层内事实-判断（呈现层）三项过闸结论。
- **§7** 版本 bump v1.2→v1.3 并注明"仅 SPEC、HTML 仍为 v1.2 内容"；顺手修正 `var()` 计数 564→565（实测值）。
- 旧版 SPEC 已备份至 `backups/AI产业框架地图_SPEC_v1.2_2026-06-07.md`。

## v1.2 · 2026-06-07（已发布）

**主题**：内容三层化 + 横向维度扩展 + CSS 基建。

- **A. 玩家信息三层化（DONE）** — 12 层玩家区全部从平铺改为「头部 / 挑战者 / 悬疑」三栏。挑战者带一句 `<em>` 关键词注释；悬疑是层内未来 12–18 个月最可能颠覆现状的问题，含 `<strong>` 关键判断与 `<em>` 推论。
- **B. 三个新横向维度（DONE）** — §03 新增 `#dim-s` 块「三个结构性力」，含 3 张 hdim-card：
  - *人才流动*（OpenAI/DeepMind→Anthropic 8:1/11:1、Karpathy 加盟、Anthropic 80% 留存）
  - *开源/闭源权重转移*（MMLU 差距 17.5pp→0.3pp、闭源占 token 80%/营收 96%、开源单 token 成本 1/6）
  - *能耗 / 碳强度*（96 GW 全球、Stargate 7 GW、Anthropic AWS $100B / 5 GW、五巨头资本支出 $660–690B）
  - dim-a 头部更名"三个治理维度"；lead 文案从"六个维度"改为"九个维度"；dim-closing 同步；TOC 加入 dim-s 入口。
- **C. CSS 尺寸 token 化（PARTIAL）** — 新增 `--nav-h --pad-section --pad-section-sm --pad-container --pad-card --gap-grid --gap-grid-sm --gap-chip --touch-target --fs-h1..h4 --fs-body --fs-small --fs-mono` 完整 token 集；1100 / 700 / 420 / 380 四个断点中**只覆盖 token 值**；`.container / h1.title / .nav-container` 等高层规则已切到 `var()`。完整移除 `!important` 留作下一版（仍约 99 处）。
- 主页面三处 `v1.0 · 2026.05` 同步 bump 为 `v1.2 · 2026.06`。
- SPEC 同步：§2 §3.3.1 §4.1 §4.2 §7 §11 全部更新；本 CHANGELOG 同步。

## v1.1 · 2026-06-07（已备份）

**主题**：评审反馈落地 —— 内容动态化与移动端优化。

- 新增 meta / OG / favicon（含 theme-color light/dark）
- `prefers-reduced-motion` 支持
- `@media (hover: none)` 触屏友好：玩家芯片 ↗ 常驻 + 触控目标 36px
- 点击拦截器去掉 stopPropagation、改冒泡阶段、内置 reduced-motion 与断点判断
- IntersectionObserver rootMargin 响应式（mqMobile 监听重建）
- `content-visibility: auto` 应用于 §03 子区、12 层卡、关系区
- `prefers-color-scheme: dark` 暗色模式（重新映射调色板，保留族群语义）
- 12 层卡片底部迷你导航（← 上一层 / ↑ 顶部 / 下一层 →）
- Pulse ↔ Layer 双向联动：自动解析 `Lx` 标签，层卡顶部生成"近期动态"小条
- 6 维层级速测（护城河/商品化/5 年风险/决策周期/资本密度/增速）
- 动态流可折叠：最新批次展开、旧批次默认折叠，顶部加视图工具条，偏好走 localStorage
- 动态流追加 2026.06.01 第 2 批 7 条
- 写第一版 SPEC.md 与 CHANGELOG.md

## v1.0 · 2026-05-26（初始版本）

**主题**：项目骨架与首期数据。

- 单文件 HTML，12 层 × 4 族群骨架
- §00 动态流（首期 9 条）/ §01 全栈 / §02 关系 / §详情 / §03 维度 / Footer
- 固定顶部导航 + 抽屉式 TOC
- 完整的桌面/平板/手机/超小屏 5 档断点
