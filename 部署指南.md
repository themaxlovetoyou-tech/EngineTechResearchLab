# 把这张地图上线到 GitHub Pages

整套流程一次性 10–15 分钟,之后每周自动更新都会同步到线上。

---

## 一、准备工作(只做一次)

### 1. 注册 GitHub 账号

打开 https://github.com/signup,用邮箱注册。用户名想清楚——它会出现在你的网址里(形如 `https://你的用户名.github.io/...`)。

### 2. 安装 Git

- **Windows**: https://git-scm.com/download/win 下载安装,一路下一步即可。
- **Mac**: 打开「终端」输入 `git --version`,如果提示安装就照做。

安装完打开终端 / 命令提示符,告诉 Git 你是谁:

```bash
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"
```

### 3. 设置 GitHub 凭据(推送时要用)

最省事的方式是装 **GitHub CLI**:https://cli.github.com

装完运行:
```bash
gh auth login
```
按提示选 `GitHub.com` → `HTTPS` → `Yes` 用浏览器登录,完成授权。之后 `git push` 就不会再问账号密码。

---

## 二、创建仓库并上传文件(只做一次)

### 1. 在 GitHub 上新建仓库

去 https://github.com/new:

- **Repository name**: 起个简短的名字,比如 `ai-stack` —— 它会成为你的网址的一部分
- **Public** ✅ (必须公开,GitHub Pages 免费版只支持公开仓库)
- 其余保持默认,**不要**勾选「Add a README」
- 点 **Create repository**

### 2. 把这个文件夹推到仓库

打开终端,`cd` 到你存放这个网页的文件夹(就是「CC造物」),然后:

```bash
git init
git add AI产业框架地图.html og.png
git commit -m "初版上线"
git branch -M main
git remote add origin https://github.com/你的用户名/ai-stack.git
git push -u origin main
```

> 顺手把 `部署指南.md` 也加上(`git add 部署指南.md`)也行,不影响。

### 3. 开启 GitHub Pages

进入你刚创建的仓库页面 → 顶部菜单 **Settings** → 左侧 **Pages** :

- **Source**: 选 `Deploy from a branch`
- **Branch**: 选 `main` / `/(root)`,点 **Save**

等 1–2 分钟,页面上方会出现绿色提示:

> Your site is live at **https://你的用户名.github.io/ai-stack/**

把这串地址记下来——但**先别急着分享**。还有一步关键收尾。

### 4. 设置默认主页(重要)

GitHub Pages 默认找的是 `index.html`,而我们的文件叫 `AI产业框架地图.html`。两个办法二选一:

**A. 改文件名(最简单)**——把 `AI产业框架地图.html` 改名为 `index.html`,然后重新推送。如果你想保留原文件名给本地阅读,看 B。

**B. 加一个跳转壳**——在仓库根目录加一个 `index.html`,内容只有:

```html
<!DOCTYPE html><meta http-equiv="refresh" content="0; url=AI产业框架地图.html">
```

把它也提交并推送。打开 `https://你的用户名.github.io/ai-stack/` 会自动跳到正文。

---

## 三、回填分享元数据(让链接预览生效)

打开 `AI产业框架地图.html`,把页头 4 处 `REPLACE-WITH-YOUR-DOMAIN` 全部替换成你的网址。例如,你的网址是 `https://maxlovetoyou.github.io/ai-stack/`,就把:

```html
<meta property="og:url"   content="https://REPLACE-WITH-YOUR-DOMAIN/">
<link  rel="canonical"    href="https://REPLACE-WITH-YOUR-DOMAIN/">
<meta property="og:image" content="https://REPLACE-WITH-YOUR-DOMAIN/og.png">
<meta name="twitter:image" content="https://REPLACE-WITH-YOUR-DOMAIN/og.png">
```

改成:

```html
<meta property="og:url"   content="https://maxlovetoyou.github.io/ai-stack/">
<link  rel="canonical"    href="https://maxlovetoyou.github.io/ai-stack/">
<meta property="og:image" content="https://maxlovetoyou.github.io/ai-stack/og.png">
<meta name="twitter:image" content="https://maxlovetoyou.github.io/ai-stack/og.png">
```

保存后再推一次:

```bash
git add AI产业框架地图.html
git commit -m "回填正式域名"
git push
```

验证分享卡片:打开 https://www.opengraph.xyz/url/你的网址 —— 应当能看到那张土黄色封面。

---

## 四、把每周自动更新接到线上(强烈推荐)

我已经为你预留了这一步——把仓库本地路径告诉定时任务,它每周更新完文件后,会自动 `git push` 上去,让线上同步刷新。

具体怎么开启:告诉我「我已经把仓库设置在 _________ 路径(完整本地路径)」,我把这条信息写进任务的 prompt,之后无需再操心。

---

## 故障排查

| 现象 | 处理 |
|---|---|
| `git push` 报「Permission denied」 | 跑一次 `gh auth login` 重新授权;或检查 `git remote -v` 里的用户名拼写。 |
| 上线后页面空白 | 浏览器开 F12 看 Console。99% 是没改默认主页(见二·4)。 |
| 分享时仍显示干巴巴的 URL | 4 处 `REPLACE-WITH-YOUR-DOMAIN` 是否都改了?改完是否 push 了?分享平台是否缓存了旧版本(微信用 https://opengraph.xyz/ 强制刷新)? |
| 中文文件名在 URL 里乱码 | 这是为什么建议改名为 `index.html` 或加跳转壳。 |
| 想换成自己的域名(如 `ai-stack.你的域名.com`) | 仓库 Settings → Pages → Custom domain 填入域名;同时去域名商加一条 CNAME 指向 `你的用户名.github.io`。 |

---

## 速查:每次想手动更新

无论是定时任务跑完,还是你自己改了什么,都用这三行:

```bash
cd 你的本地仓库路径
git add .
git commit -m "更新说明"
git push
```

约 30 秒后线上生效。
